#include <stdio.h>

/**
 * main - entry point
 *
 * Description: code to print num
 *
 * Return: alwasy have value of 0
*/

int main(void)
{
	int i;

	for (i = '0'; i <= '9';(int)i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
