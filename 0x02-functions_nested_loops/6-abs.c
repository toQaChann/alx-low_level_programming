#include "main.h"

/**
 *_abs - check case
 *@n: the character that will be checked
 *
 *Return: will return the value of operation
*/

int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	else
	{
		return (n);
	}
}
